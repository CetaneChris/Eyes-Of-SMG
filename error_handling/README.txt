To run this program:
    1. Make sure node and npm is installed.
    2. Install mysql and twilio package using "npm install [package name]".
    3. On a linux system/shell, run "watch -n 10 script.sh" command.